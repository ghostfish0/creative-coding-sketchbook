const treeHeight = 7;
const groups = 2;
const levelHeight = 20;
const canvasWidth = groups * (1 << treeHeight);
const canvasHeight = levelHeight * treeHeight;

themecolors = [ 
    [0, 0, 0],
    [0, 255, 0] 
];

